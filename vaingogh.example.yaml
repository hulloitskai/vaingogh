# Server options:
server:
  baseURL: String

# Watcher options:
watcher:
  checkInterval: Duration

# GitHub options:
github:
  username: String

  # Repository lister options:
  lister:
    concurrency: Int
