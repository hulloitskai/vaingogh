# Server options:
server:
  baseURL: String
  shutdownTimeout: Duration

# Watcher options:
watcher:
  checkInterval: Duration

# Lister options:
lister:
  concurrency: Int

  # GitHub options:
  github:
    username: String
